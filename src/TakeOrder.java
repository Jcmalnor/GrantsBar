
public class TakeOrder {
	/*
	 * create method getTakeOrder
	 * create variable
	 * prompt user to input order and quantity
	 * store user input (drink selection)
	 * prompt user for more menu items
	 * 	if yes show menu again
	 * 	if no break;
	 * prompt user for tip
	 * calculate total = line price * quantity
	 * add tax and tip
	 * return total
	 */
}
